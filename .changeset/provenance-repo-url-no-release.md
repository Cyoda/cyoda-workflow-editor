---
---

Correct the published `repository.url` org slug (`Cyoda-platform` → `Cyoda`) so
npm sigstore provenance verification passes. Metadata-only; intentionally an
empty changeset (no version bump) — this republishes the existing 0.3.0 line
that failed to publish on its first attempt.
